# Base URL of the poultry-farm API (Fastify). Used by src/services/api-client.ts.
# In Expo, only EXPO_PUBLIC_* variables are inlined into the client bundle.
# The port must match the API's own PORT (default 4000 in apps/api/src/config/env.ts).
EXPO_PUBLIC_API_URL=http://localhost:4000